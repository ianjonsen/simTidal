% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_par.R
\name{drifter_par}
\alias{drifter_par}
\title{Parameters for passive drifter simulation}
\usage{
drifter_par(
  N,
  time.step = 10,
  start.dt = NULL,
  start = c(0, 0),
  noise = NULL,
  beta = c(-0.15, -0.15),
  buffer = 1,
  uvm = c(1, 1),
  advect = TRUE,
  interp = FALSE
)
}
\arguments{
\item{N}{Number of simulation time steps (required, positive integer)}

\item{time.step}{Duration of each step in minutes (default 10, must match
FVCOM output interval)}

\item{start.dt}{Simulation start datetime (POSIXct, UTC). Defaults to
\code{NULL}, resolved to the first FVCOM timestamp
(\code{data$fvcom.origin}) inside \code{sim_drifter()}}

\item{start}{Start coordinates, c(x, y) in km (UTM Zone 20N)}

\item{noise}{Optional positional noise: sd in km added to each step.
\code{NULL} (default) = purely advection-driven}

\item{beta}{Land-avoidance potential function coefficients, both <= 0.
More negative = stronger repulsion from land.
Default \code{c(-0.15, -0.15)}}

\item{buffer}{Search radius (km) for water when drifter grounds on land}

\item{uvm}{Current multiplier: scalar or c(u_mult, v_mult).
Default \code{c(1, 1)} = unmodified FVCOM currents}

\item{advect}{Logical; advect by FVCOM currents (default \code{TRUE})}

\item{interp}{Logical; if \code{TRUE}, linearly interpolate u and v
between the two FVCOM layers bracketing \code{start.dt}
(2 extracts per step). If \code{FALSE} (default), snap
\code{start.dt} to the nearest layer boundary and use
a single extract per step.}
}
\value{
Object of class \code{c("drifter_par", "mpar")}
}
\description{
Construct and validate parameters for \code{sim_drifter()}
  with \code{move = "drifter"}. The drifter is advected entirely by FVCOM
  currents; optional Gaussian noise can be added to each position.
}
\examples{
## Snap to nearest boundary (default — fastest)
mpar <- drifter_par(N = 1440, start = c(512.3, 214.8))

## Linear interpolation between layers
mpar <- drifter_par(N = 1440, start = c(512.3, 214.8), interp = TRUE)

}
