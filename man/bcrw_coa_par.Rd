% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_par.R
\name{bcrw_coa_par}
\alias{bcrw_coa_par}
\title{Parameters for biased correlated random walk toward a Centre of Attraction}
\usage{
bcrw_coa_par(
  N,
  time.step = 10,
  start.dt = NULL,
  start = c(0, 0),
  coa = cbind(c(0, 0)),
  coa.tol = 1,
  nu = 1,
  rho = 0.5,
  bl = 1.5,
  fl = 0.15,
  noise = NULL,
  beta = c(-0.15, -0.15),
  buffer = 1,
  uvm = c(1, 1),
  advect = TRUE,
  interp = FALSE
)
}
\arguments{
\item{N}{Number of simulation time steps (required)}

\item{time.step}{Step duration in minutes (default 10)}

\item{start.dt}{Simulation start datetime (POSIXct, UTC). Defaults to
\code{NULL}, resolved to the first FVCOM timestamp
(\code{data$fvcom.origin}) inside \code{sim_drifter()}}

\item{start}{Start coordinates, c(x, y) in km}

\item{coa}{CoA coordinates. Either c(x, y) for a single CoA, or a
2-column matrix with one row per sequential CoA}

\item{coa.tol}{Tolerance radius (km) around each CoA}

\item{nu}{Strength of attraction toward CoA (>= 0)}

\item{rho}{Angular concentration (Wrapped Cauchy), range [0, 1)}

\item{bl}{Swimming speed in body-lengths per second}

\item{fl}{Fork length in metres}

\item{noise}{Optional positional noise sd in km. \code{NULL} = off}

\item{beta}{Land-avoidance coefficients, both <= 0}

\item{buffer}{Water-search radius (km) when grounded}

\item{uvm}{Current multiplier: scalar or c(u_mult, v_mult)}

\item{advect}{Logical; advect by FVCOM currents (default \code{TRUE})}

\item{interp}{Logical; if \code{TRUE}, linearly interpolate u and v
between the two FVCOM layers bracketing \code{start.dt}.
Default \code{FALSE}.}
}
\value{
Object of class \code{c("bcrw_coa_par", "mpar")}
}
\description{
Construct and validate parameters for \code{sim_drifter()}
  with \code{move = "bcrw.coa"}. Swimming is biased toward one or more
  sequential Centres of Attraction, with FVCOM advection overlaid.
}
